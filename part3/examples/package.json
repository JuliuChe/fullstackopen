{
  "name": "backend",
  "version": "0.0.1",
  "description": "example from the course",
  "license": "MIT",
  "author": "Julien Chevalley",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "lint" : "eslint .",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "chrome-dev": "node --inspect index.js",
    "build:ui": "rm -rf dist && cd ../phonebook_frontend && npm run build && cp -r dist ../phonebook_backend && cd ../phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m  \"${npm_config_message:-uibuild}\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.0.3",
    "globals": "^17.4.0"
  }
}
