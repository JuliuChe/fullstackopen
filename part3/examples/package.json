{
  "name": "backend",
  "version": "0.0.1",
  "description": "example from the course",
  "license": "MIT",
  "author": "Julien Chevalley",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../notes-frontend && npm run build && @powershell cp -r dist ../examples && cd ../examples",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
